---
- debug: msg="START netconf_rpc junos/basic.yaml on connection={{ ansible_connection }}"

- name: discard changes
  network.netconf.netconf_rpc:
    rpc: discard-changes

- debug: msg="END netconf_rpc junos/basic.yaml on connection={{ ansible_connection }}"
